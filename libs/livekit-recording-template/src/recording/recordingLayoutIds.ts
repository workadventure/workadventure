/** Must match `LIVEKIT_RECORDING_LAYOUT_SPEAKER` on the back when starting egress. */
export const RECORDING_LAYOUT_SPEAKER = "wa-speaker";

/** Must match `LIVEKIT_RECORDING_LAYOUT_LIVEKIT_FULLSCREEN` on the back. Uses `@livekit/components-react` (single track). */
export const RECORDING_LAYOUT_LIVEKIT_FULLSCREEN = "wa-livekit-fullscreen";
