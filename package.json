{
  "private": true,
  "devDependencies": {
    "husky": "^7.0.1",
    "path-exists-cli": "^2.0.0"
  },
  "scripts": {
    "prepare": "path-exists node_modules/.bin/husky && husky install || echo 'Husky not found. Maybe you are installing only a subproject.'"
  },
  "workspaces": [
    "play",
    "back",
    "chat",
    "map-storage",
    "libs/*",
    "benchmark",
    "uploader",
    "play/packages/iframe-api-typings"
  ]
}
