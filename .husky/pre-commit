#!/bin/sh
. "$(dirname "$0")/_/husky.sh"
(
cd messages || exit
pnpm run precommit
)
(
cd play || exit
pnpm run precommit
)
(
cd back || exit
pnpm run precommit
)
(
cd map-storage || exit
pnpm run precommit
)
(
cd tests || exit
pnpm run precommit
)
