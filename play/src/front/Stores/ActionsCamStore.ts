import {writable } from "svelte/store";

export const lightMode = writable(true);
export const rightMode = writable(true);
export const focusMode = writable(true);
export const hideMode = writable(false);
