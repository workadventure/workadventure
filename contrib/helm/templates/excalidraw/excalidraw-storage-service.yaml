{{- if .Values.excalidraw.enabled }}
---
apiVersion: v1
kind: Service
metadata:
  name: {{ include "workadventure.fullname" . }}-excalidraw-storage
  labels:
    {{- include "workadventure.labels" . | nindent 4 }}
    role: excalidraw-storage
spec:
  type: {{ .Values.excalidraw.excalidrawStorage.service.type }}
  ports:
    - port: {{ .Values.excalidraw.excalidrawStorage.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "workadventure.selectorLabels" . | nindent 4 }}
    role: excalidraw-storage
{{ end -}}
